# Requirements for Streamlit Cloud deployment (Python 3.11 recommended)
# Core dependencies
streamlit>=1.31.0
numpy>=1.24.0
pandas>=2.0.0
python-dotenv==1.0.0
openpyxl==3.1.2

# Embeddings + model
# Pin to stable, CPU-friendly versions with available wheels on Python 3.11
sentence-transformers==2.2.2
torch==2.2.2

# Vector index (optional) — comment out if wheels unavailable in your environment
# faiss-cpu==1.7.4

# Optional: For Flask API
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

